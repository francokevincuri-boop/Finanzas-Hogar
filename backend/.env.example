# Copiar este archivo a .env (el .env real nunca se sube a git)
DATABASE_URL="postgresql://finanzas:finanzas@localhost:5432/finanzas_hogar?schema=public"
JWT_SECRET="cambiar-por-un-secreto-largo-y-aleatorio"
PORT=3000
